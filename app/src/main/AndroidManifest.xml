<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bridgelabz.fundoonotes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".note_module.dashboard_page.view.HomeDashBoardActivity"
            android:label="@string/title_activity_home_dash_board"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".user_module.lunch.LaunchScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".note_module.dashboard_page.view.DashBoardActivity" />
        <activity android:name=".user_module.registration.view.RegisterActivity" />
        <activity android:name=".user_module.login.view.LoginActivity" />
    </application>

</manifest>